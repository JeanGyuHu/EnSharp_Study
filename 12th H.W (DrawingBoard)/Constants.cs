using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Hu_s_Drawing_Board
{
    class Constants
    {
        public enum MODE
        {
            PEN = 0, RECT, CIRCLE, LINE, ERASE, SELECT, SPOID, PAINT
        }
        
        public enum THICK
        {
            ONE = 1,TWO, THREE, FOUR, FIVE, SIX
        }
    }
}
